// import React from "react";


// function Card({ card, handleNext, handlePrev }) {
//   const handleNextClick = () => {
//     handleNext(card.id);
//   };

//   const handlePrevClick = () => {
//     handlePrev(card.id);
//   };

//   return (
//     <div className="card">
//       <p>{card.name}</p>
//       {handlePrev && <button onClick={handlePrevClick}>Previous</button>}
//       {handleNext && <button onClick={handleNextClick}>Next</button>}
//     </div>
//   );
// }

// export default Card;